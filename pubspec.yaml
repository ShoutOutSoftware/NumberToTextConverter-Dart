name: number_to_text_converter
description: A library to convert a number to text
version: 1.0.0
homepage: https://github.com/ShoutOutSoftware/NumberToTextConverter-Dart

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.16.1
  pedantic: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter